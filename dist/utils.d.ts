/**
 * Returns `true` if the accessibility check is successful.
 */
export declare function isExists(filepath: string): Promise<boolean>;
/**
 * Returns `true` if the sync accessibility check is successful.
 */
export declare function isExistsSync(filepath: string): boolean;
