{
  "name": "rcfy",
  "description": "Finds and loads runtime-configuration file for the current project with precedence",
  "version": "0.0.0-development",
  "publishConfig": {
    "access": "public"
  },
  "author": "Beni Arisandi (https://stilearning.com)",
  "repository": "https://github.com/bent10/rcfy.git",
  "license": "MIT",
  "keywords": [
    "rc",
    "runtime",
    "config",
    "configuration",
    "control",
    "run",
    "command",
    "fs",
    "load",
    "loader"
  ],
  "engines": {
    "node": ">=12"
  },
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "readme.md",
    "license"
  ],
  "scripts": {
    "start": "npm run build && doogu watch",
    "lint": "doogu lint --types",
    "build": "doogu build --types",
    "test": "ava --verbose",
    "coverage": "c8 -r lcov -r text npm test"
  },
  "dependencies": {
    "loadee": "^2.1.1"
  },
  "devDependencies": {
    "ava": "4.2.0",
    "c8": "7.11.2",
    "doogu": "1.2.0",
    "ts-node": "10.7.0"
  },
  "ava": {
    "files": [
      "test/**/*.test.ts"
    ],
    "extensions": {
      "ts": "module"
    },
    "experiments": {
      "configurableModuleFormat": true
    },
    "nodeArguments": [
      "--loader=ts-node/esm",
      "--no-warnings"
    ],
    "ignoredByWatcher": [
      "{coverage,dist}/**"
    ]
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  },
  "release": {
    "branches": [
      "+([0-9])?(.{+([0-9]),x}).x",
      "main",
      "next",
      "next-major",
      {
        "name": "beta",
        "prerelease": true
      },
      {
        "name": "alpha",
        "prerelease": true
      }
    ],
    "plugins": [
      "@semantic-release/commit-analyzer",
      "@semantic-release/release-notes-generator",
      [
        "@semantic-release/changelog",
        {
          "changelogFile": "changelog.md"
        }
      ],
      [
        "@semantic-release/git",
        {
          "assets": [
            "changelog.md"
          ],
          "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
        }
      ],
      "@semantic-release/github",
      "@semantic-release/npm"
    ]
  },
  "foo": {
    "bar": "bar",
    "corge": {
      "xyz": 123
    }
  }
}
